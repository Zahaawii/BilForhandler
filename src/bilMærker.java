public class bilMærker {
    String [] bilmærker = {"", "1. Ferrari", "2. Lamborghini", "3. Porshce", "4. Mercedes"};

    bilMærker () {
        for(int i = 0; i < bilmærker.length; i++){
            System.out.println(bilmærker[i]);
        }
    }
}
